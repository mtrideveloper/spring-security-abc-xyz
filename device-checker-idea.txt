User non-logged:
	+ Trang chủ gồm: Check device status, Analysis problems from image, Part Finder By Device Name (tìm linh kiện phù hợp theo tên)
	
	+ Kiểm tra tình trạng thiết bị, frontend tự detect device là PC, ipad hay smartphone
	
	+ Kết quả trả về, để lưu tình trạng (gồm thời gian) hiện tại của device, user phải login
	
	+ Để dùng chức năng phân tích vấn đề từ hình ảnh (Analysis problems from image), yêu cầu login.

User logged:
	+ Analysis problems from image -> Kết quả, giải pháp đề xuất
	
	+ Part Finder By Device Name
	
	+ Hiển thị tình trạng device và thời gian trả về kết quả này
	
	+ Hiển thị lịch sử phân tích (thời gian, ảnh, kết quả, đề xuất).
	
	* Mỗi lần user thực hiện 1 trong 2 chức năng, 1 dialog hiện lên thông báo rằng "dữ liệu sẽ được ghi lên server, bạn có đồng ý?"
		
Admin:
	+ Hiển thị danh sách user
	+ Hiển thị chi tiết lịch sử Check device status, Analysis problems from image của user logged
	+ Xóa user.